# Example environment variables for Smart-parking_management_App
# Copy this file to `.env` and fill in real values (do NOT commit `.env`)

# Database
DB_HOST=localhost
DB_USER=root
DB_PASSWORD='your_password_here'
DB_NAME=smart_spaces
DB_PORT=3306

# Flask / App
FLASK_DEBUG=True
SECRET_KEY=change-me-for-dev
